# Core ML stack
numpy>=1.24,<2.2
pandas>=2.0,<2.3
scipy>=1.10
scikit-learn>=1.3,<1.6
joblib>=1.3

# Drift detection & monitoring
evidently>=0.4.0,<0.5   # classic Report / metric_preset API used in src/ml_drift/drift/reports.py

# Config & CLI
PyYAML>=6.0

# Visualisation / dashboard (optional but used by app + monitoring)
matplotlib>=3.7
streamlit>=1.30

# Testing / quality
pytest>=7.4
pytest-cov>=4.1
